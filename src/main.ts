import { Blockchain } from "./Blockchain";

const blockchain = new Blockchain();

blockchain.mineBlock({
  amount: 1,
});
blockchain.mineBlock({
  amount: 1,
});
blockchain.mineBlock({
  amount: 1,
});
